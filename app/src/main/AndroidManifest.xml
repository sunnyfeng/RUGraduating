<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunnyfeng.rugraduating" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".objects.User"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuildStudentActivity" android:theme="@style/Theme.AppCompat.Translucent" >

        </activity>
        <activity android:name=".MajorActivity" ></activity>
        <activity android:name=".TopViewActivity" >
        </activity>
        <activity android:name=".CourseInfoActivity" > </activity>
        <activity android:name=".EquivInfoActivity" > </activity>
        <activity android:name=".RegexInfoActivity" > </activity>
        <activity android:name=".AddClassesActivity" >
            </activity>
        <activity android:name=".SuggestedCoursesActivity" >

        </activity>
        <activity android:name=".ProfileActivity" ></activity>
        <activity android:name=".RequirementsActivity" > </activity>
        <activity android:name=".SignUpActivity" >

        </activity>
    </application>

</manifest>